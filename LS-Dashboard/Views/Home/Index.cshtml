@{
    ViewBag.Title = "Home";
}

<div>

</div>

@section scripts
{
    @Scripts.Render("~/Scripts/jquery.canvasjs.js")
    @Scripts.Render("~/Scripts/jquery.signalR-2.2.0.js")
    @Scripts.Render("~/signalr/hubs")
    <script>
        var proxy = $.connection.ServerHub;

        $.extend(proxy.client, {
            updateIncident: function (incident) {
                
            }
        });

        $.connection.hub.start().done(function () {
            
        }).fail(function () {
            
        });
    </script>
}